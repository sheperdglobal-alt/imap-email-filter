# IMAP Email Filter with PDF OCR - Dependencies

# IMAP and Email handling
imaplib2==3.6
email-validator==2.0.0

# PDF processing and OCR
pdfplumber==0.11.0
pytesseract==0.3.10
Pillow==10.1.0
pdf2image==1.16.3

# Environment and configuration
python-dotenv==1.0.0

# Data handling
python-dateutil==2.8.2

# Logging (built-in, no install needed)
# json (built-in)
# datetime (built-in)
# re (built-in)
# os (built-in)
# logging (built-in)
